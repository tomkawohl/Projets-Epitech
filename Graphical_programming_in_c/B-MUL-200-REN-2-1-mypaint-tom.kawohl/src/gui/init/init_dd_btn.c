/*
** EPITECH PROJECT, 2023
** src/gui/init/init_main_btn
** File description:
** this will initialize the dropdown buttons
*/

#include "my_paint.h"
/*
void add_options(paint_t *info, int index, char *titles)
{
    char **arr = my_strtok(titles, ':');
    int len = get_arrlen(arr);
    options_t *options = malloc(sizeof(struct options_t) * len);

    info->gui.topbar->dropdown_btn[index].options_len = len;
}

void save_to_file(paint_t *info)
{

}

dropdown_btn_t *init_dd_btn(paint_t *info, topbar_t *topbar)
{
    dropdown_btn_t *dropdown_btn = malloc(sizeof(struct dropdown_btn_t)
        * (TOPBTN_LEN));

    topbar->curr_left = 3.0;
    topbar->dropdown_btn = dropdown_btn;
    add_main_btn(topbar, 0, "File");
    add_options(info, 0, "Save:Exit");
    //add_options(info, (two_int_t){0, 1}, "Exit", exit_paint);
    add_main_btn(topbar, 1, "Edit");
    //add_options(info, (two_int_t){1, 0}, "Undo", undo);
    //add_options(info, (two_int_t){1, 1}, "Redo", redo);
    //add_options(info, (two_int_t){1, 2}, "Use Pencil", equip_pencil);
    //add_options(info, (two_int_t){1, 3}, "Use Eraser", equip_eraser);
    return dropdown_btn;
}
*/
