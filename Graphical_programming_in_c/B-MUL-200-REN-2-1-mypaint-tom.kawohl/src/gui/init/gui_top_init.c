/*
** EPITECH PROJECT, 2023
** src/gui/gui_top_init
** File description:
** top part of the gui
*/

#include "my_paint.h"
/*
void gui_top_init(paint_t *info)
{
    info->gui.topbar = malloc(sizeof(struct topbar_t));
    info->gui.topbar->background = sfRectangleShape_create();
    sfRectangleShape_setPosition(info->gui.topbar->background,
        (sfVector2f) {0.0, 0.0});
    sfRectangleShape_setFillColor(info->gui.topbar->background, TOP_BG);
    sfRectangleShape_setSize(info->gui.topbar->background, (sfVector2f)
        {(float) TOPBAR_HEIGHT, WIDTH});
    info->gui.topbar->dropdown_btn = init_dd_btn(info, info->gui.topbar);
}
*/
